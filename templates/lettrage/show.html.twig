{% extends 'base.html.twig' %}

{% block title %}Lettrage{% endblock %}

{% block body %}
    <h1>Lettrage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lettrage.id }}</td>
            </tr>
            <tr>
                <th>RecordedAt</th>
                <td>{{ lettrage.recordedAt ? lettrage.recordedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ lettrage.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lettrage_index') }}">back to list</a>

    <a href="{{ path('app_lettrage_edit', {'id': lettrage.id}) }}">edit</a>

    {{ include('lettrage/_delete_form.html.twig') }}
{% endblock %}
